/**
 * Punct unic de import pentru catalogul de rețete (alias: @/lib/recipes).
 * Datele rămân definite în app/lib/recipesDatabase.ts.
 */
export {
  RECIPES,
  type MealType,
  type RecipeCatalogItem,
} from "../app/lib/recipesDatabase";
